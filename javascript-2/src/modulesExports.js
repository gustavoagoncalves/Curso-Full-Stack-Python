// Named Exports. Sem necessidade de usar o export
export let cores = []
export function getName(){}
export class Animal{}

// Lista de exportação
export { cores, getName, Animal}

// strict mode > como ficam todos os exports

//export default getCars(){}

// export default getCars (apenas um default por arquivo)